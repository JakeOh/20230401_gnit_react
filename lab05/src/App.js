import IterationExample from './IterationExample';

function App() {
  const items = ['React', 'Vue.js', 'Angualr.js', 'jQuery'];

  return (
    <div className="App">
      <IterationExample items={items} />
    </div>
  );
}

export default App;
